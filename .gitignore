# ==============================================================================
# .gitignore - Git 버전 관리 제외 목록
# ==============================================================================
# 이 파일은 Git이 추적하지 말아야 할 파일 및 폴더 목록을 정의합니다.
# 여기에 명시된 파일들은 원격 저장소(GitHub 등)에 업로드되지 않습니다.
# ------------------------------------------------------------------------------

# --- 민감한 정보 파일 ---
# config.ini 파일은 DB 접속 정보, API 키 등 민감한 정보를 포함하므로
# 보안을 위해 버전 관리에서 제외합니다.
config.ini

# --- 파이썬 캐시 파일 ---
# 파이썬 실행 시 자동으로 생성되는 캐시 폴더 및 파일을 제외합니다.
# 이 파일들은 실행 속도 향상을 위해 사용되지만, 프로젝트 코드와는 무관합니다.
__pycache__/
*.pyc

# --- 데이터베이스 파일 ---
# 로컬에서 사용하는 데이터베이스 파일(예: SQLite)이 있다면 제외합니다.
# 대용량 데이터 파일은 Git으로 관리하기에 적합하지 않습니다.
*.db
*.csv